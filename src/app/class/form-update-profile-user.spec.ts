import { FormUpdateProfileUser } from './form-update-profile-user';

describe('FormUpdateProfileUser', () => {
  it('should create an instance', () => {
    expect(new FormUpdateProfileUser()).toBeTruthy();
  });
});
