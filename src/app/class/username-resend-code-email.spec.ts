import { UsernameResendCodeEmail } from './username-resend-code-email';

describe('UsernameResendCodeEmail', () => {
  it('should create an instance', () => {
    expect(new UsernameResendCodeEmail()).toBeTruthy();
  });
});
