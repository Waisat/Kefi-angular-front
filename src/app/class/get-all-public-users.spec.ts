import { GetAllPublicUsers } from './get-all-public-users';

describe('GetAllPublicUsers', () => {
  it('should create an instance', () => {
    expect(new GetAllPublicUsers()).toBeTruthy();
  });
});
