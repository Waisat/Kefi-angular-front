import { NavPageMemberList } from './nav-page-member-list';

describe('NavPageMemberList', () => {
  it('should create an instance', () => {
    expect(new NavPageMemberList()).toBeTruthy();
  });
});
