import { FoundersPublicModel } from './founders-public-model';

describe('FoundersPublicModel', () => {
  it('should create an instance', () => {
    expect(new FoundersPublicModel()).toBeTruthy();
  });
});
