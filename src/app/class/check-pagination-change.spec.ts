import { CheckPaginationChange } from './check-pagination-change';

describe('CheckPaginationChange', () => {
  it('should create an instance', () => {
    expect(new CheckPaginationChange()).toBeTruthy();
  });
});
