import { DetailUserPublicArea } from './detail-user-public-area';

describe('DetailUserPublicArea', () => {
  it('should create an instance', () => {
    expect(new DetailUserPublicArea()).toBeTruthy();
  });
});
