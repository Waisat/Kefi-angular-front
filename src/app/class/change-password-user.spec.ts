import { ChangePasswordUser } from './change-password-user';

describe('ChangePasswordUser', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordUser()).toBeTruthy();
  });
});
