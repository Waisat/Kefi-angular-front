import { FounderDetailPublic } from './founder-detail-public';

describe('FounderDetailPublic', () => {
  it('should create an instance', () => {
    expect(new FounderDetailPublic()).toBeTruthy();
  });
});
