import { GetTokenArticleDetail } from './get-token-article-detail';

describe('GetTokenArticleDetail', () => {
  it('should create an instance', () => {
    expect(new GetTokenArticleDetail()).toBeTruthy();
  });
});
